# ADR-001: uni-app Vue 3 and TypeScript baseline

Status: Accepted

Use the official uni-app Vue 3 CLI model with Vite and TypeScript. The App target is the product target; H5 is the deterministic CI and prototype-review target.

DCloud package versions must be pinned to one matching release identifier. Mixed DCloud release identifiers are forbidden.
